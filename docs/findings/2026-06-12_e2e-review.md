# 2026-06-12 — End-to-end review (Criticals + H/M/L backlog)

Independent end-to-end review (dataflow tracing + live-state reconciliation,
not layer-level). Two Criticals + High/Medium/Low. **Criticals C1/C2/C3 are
shipped + validated** (see status tags). This doc is the durable backlog; each
open item is a separate PR with its own measurement gate (one-behavior-per-PR).

> "Every Critical here is a composition failure, not a bad function … only
> end-to-end dataflow tracing plus live-state reconciliation catches these."

## Criticals — DONE

- **C1 — calibration-counter ratchet.** ✅ `a3abae5` + repair. DRY_RUN archive
  no longer re-merges; index versioned + sanitized; one-shot repaired 89 rows
  (32 inflated); tiers recomputed (`tariff::h7d` demoted); 7 legit adult rules.
  Validated: re-run shows 0 inflated; held after a live pflag run.
- **C2 — maturity-gate scope.** ✅ core `bd4abf4` + pflag `2f28a72`.
  (a) maturity checked at the emitted horizon only; (b) tradeable-instrument
  guard at L2+L3 (shared `agents/_instruments.py`); pflag: shared
  `agents/_maturity.py` gate, `upsert` no longer promotes on stale PF,
  `recompute_rule_payoff` is the authoritative fresh-PF flag writer. Flagged the
  5 pre-existing leaked setups (0 open positions downstream).
  Accepted residual (documented in `price_agent`): demotion-lag window on a
  *legitimately*-adult rule (near-zero risk on robust PF rules).
- **C3 — swallowed-error trio.** ✅ `6b0359c`. L3/L4 write losses + price_agent
  reconcile crash + signal-outcome write losses now record `partial`+err+meta;
  new `agents/pulsecheck/trade_layers.py` owns L3/L4 run health.

## HIGH

- **H1 — pseudo-replication: calibration n is not independent evidence.** OPEN.
  61–93% of closed rows are duplicated (ticker, entry-day) pairs (defense
  young_adult: 49 trades = 12 pairs/3 tickers; `news_article:neutral:h7d` 1,708
  trades over 13 distinct days). Fix: effective-n (distinct ticker-day / event
  cluster) in every n-based gate, or dedupe at trade-open per (rule,ticker,day).
  Verify: duplication query per rule; gates re-evaluated on effective-n.
- **H2 — after-hours entry leakage residual.** OPEN. Events 16:00–19:59 ET
  anchor to that same day's 16:00 close (a pre-event price) — flatters the
  8-K/earnings rules nearest the gate (`event_paper_agent.py:346-368`). Fix:
  bump anchor a day when event time ≥16:00 ET. Verify: recompute 8-K cohort PF
  excluding the first overnight gap.
- **H3 — streaming-mean + counter corruption.** ✅ DONE. Subsumed by C1's
  population recompute; the cache-refresh mean fix landed in C2-pflag (`upsert`
  returns counters; reconcile cache carries the updated streaming mean).
- **H4 — old adult gate (acc≥0.90/n≥30/PF>1.5) in multiple writers + dashboard.**
  PARTIAL. ✅ DB writers fixed (`backfill_paper_trades`, `recompute_maturity_flags`
  now import the shared `agents/_maturity.py`; backfill's drifted adult gate
  removed). OPEN (display/report drift, not data corruption):
  `scripts/learning_snapshot.py:113-118` still uses the old gate + an
  accuracy-based near-threshold surface (needs rework for the payoff-first gate);
  `site_generator.py` publishes `min_accuracy: 0.90` in status.json + cites the
  old gate (627, 1254-1262, 1423-1435, 1762). Also: backfill's unpaginated
  `limit:5000` read. Verify: grep `0.90`/`1.5` gate literals → only `_maturity.py`.
- **H5 — `retry_dispatch_failed` not lane-scoped** (`thesis_agent.py:1928-1943`).
  OPEN. Latent (0 dispatch_failed live) but a Telegram outage queues foreign-lane
  failures that burn thesis's 5/day budget — the 5/22–6/2 silence via a new door.
  Fix: add `model_version=eq.{MODEL_VERSION}` (1 line) + decide foreign-lane
  sweeper. Verify: mirror `test_l3_input_filter.py`.
- **H6 — risk_agent portfolio state on a truncated page.** OPEN.
  `n_closed_30d` = exactly 2000 vs 8,246 true; open-trades read capped 2000 vs
  5,804 (`risk_agent.py:314-318`) — drawdown breaker + concentration cap see
  ~24–34% of the population. Fix: paginate (`59b0324` helper exists) or
  `count=exact` aggregates. Verify: next run's `n_closed_30d ≈ live count`.
- **H7 — watchdog/dashboard false-stale cluster.** OPEN. Orchestrator flags
  intraday stale every trading morning (2h budget vs overnight gap);
  `status=running` counts as stale so site_generator always flags itself
  (`site_generator.py:1150`); price_agent watchdogs never tightened after the
  2h-cadence bump (28h/1440min — a new 513-class stall hides a day+);
  learning_snapshot has no watchdog. Fix: session-aware budgets; fresh-running =
  healthy; align rule-#9's four places for price_agent; add a snapshot
  expectation. Verify: next 04:30 UTC orchestrator flags nothing falsely.

## MEDIUM

- **M1** — backtester upserts into live `stock_agent_weights` unmarked; thesis
  reads latest-date blind (`backtester.py:764-784`). Fix: source column / date-guard.
- **M2** — archive deletion (once live) erodes the metalabel validator's corpus
  to 90d while the live gate reads cumulative calibration → the 7/06 re-run would
  be underpowered (`archive_agent.py:68-75`, `validate_metalabel_gate.py:159-163`).
  Fix: validator merges archived JSONL. *(Precondition for the 7/06 funnel re-run.)*
- **M3** — walk-forward gate optimistic: `exit_at` stamped midnight UTC +
  reconcile latency ignored (`price_agent.py:813`, `_metalabel_gate.py:84-86`).
  Fix: require `exit_at + 1 trading day < as_of`. *(Precondition for 7/06.)*
- **M4** — all L1 ingest agents finish `ok` on event-bus write failure; no
  event-volume pulse; EDGAR all-429 sweep reads healthy (`filing_agent.py:319-322`,
  `pulsecheck/ingest_agents.py:41-58`). Fix: partial + bus-volume pulsecheck.
  *(Same swallow class as C3, on the ingest/read side — see also the deferred
  sb_get read-collapse from C3.)*
- **M5** — realistic_loop mark non-atomic (mid-loop crash leaks cash forever);
  agent-path backfill trades untagged (`realistic_loop_agent.py:405-435`,
  `event_paper_agent.py:399`). Fix: recompute state from ledger each run; tag backfills.
- **M6** — backtester partial-insert zips audit rows to wrong signals; flows 13F
  prior-snapshot truncation can fabricate events; market_scanner NULL
  prior_event_id duplicates on rerun (`backtester.py:735-745`,
  `flows_agent.py:314-336`, `sql/0013`). Fix: per-chunk zip; paginate; NULLS NOT DISTINCT.
- **M7** — egress 3.92 GB/mo = 78% of budget; estimator model stale
  (site_generator 1 vs 5 runs/day, pulsecheck unmodeled); its "blob WORTH
  building" verdict contradicts CLAUDE.md's documented rejection; rule #6's "all
  workflows cancel-in-progress" false for 7 pinged workflows; bootstrap file says
  5-min for the hot four but measured ≈300/day (provisioned state ≠ file). Fix:
  fix docs + estimator; reconcile bootstrap with provisioned state before re-running.
- **M8** — 3 MACRO signals stuck `sent` since 5/12 (lane-unscoped
  `fetch_mature_signals`, no bars); dead telegram_dispatcher inventory entry;
  `_lanes.py` says "two producers" but there are 9; `count_open_signals`
  lane-unscoped; pulse alerts have no consumer. Fix: scoping fixes; register
  lanes; route pulse criticals to Telegram.

## LOW

Stale 42P10/index folklore in comments (`filing_agent:314`,
`telegram_dispatcher:106`, `event_paper:233`); CLAUDE.md rule #1's intraday
parenthetical is wrong (intraday correctly uses `event_at`); v6 bump missed 3
files vs its commit message; `is_high_conviction` computed but never persisted;
`PF=inf` passes the metalabel gate (document); `L3_INPUT_STATUSES` drops
>24h-old retried signals (document the trade-off).

## What's healthy (verified)

L3 boundary (lane+status filter + starvation canary + CI-pinned constants) is
the best seam in the repo. FIX-1A/1B/2, pagination helper, per-lane caps,
insert-failure surfacing all verified landed + correct. Zero stuck trades;
thesis lane alive; 0 NULL-outcome contamination; no 42P10 risk. Loop isolation
holds. Metalabel gate design is sound (fail-open-to-WATCH, leakage purge,
honest right-censoring; "inconclusive on <90d" was correct).

## Maturity path (honest)

The first BUY/SELL didn't graduate — it leaked 6× through C1+C2 (now fixed). The
genuine candidate is the 8-K family at h7/15/30 (true n 1123–1174, PF 2.0–2.6,
mean +1.5–3.4%) — but those are horizons the system **cannot emit or grade yet**
(everything is h1d on daily closes). No h1d cell legitimately qualifies today,
and H1/H2 both flatter the family's numbers. Realistic path: (done) repair
counters → (done) fix h1d-cell licensing → fix H1/H2 → either wait for an h1d
cell to mature on clean effective-n data, or do the horizon-aware emission
refactor (the larger, honest unlock — where the edge measurably lives).

## Meta-labeling funnel — go-live criteria

Preconditions: C1 (done), **M2** + **M3** must land or the 2026-07-06 re-run is
underpowered/optimistic. Then: ≥30 labeled outcomes per arm per horizon
(post-PR1A); ACT > SUPPRESSED mean return, bootstrap 90% CI excluding zero on ≥2
horizons; same-sign separation across 90d/180d × pf_bar ∈ {1.3,1.5,2.0};
fail-open share <~60–80%; 2–4 weeks shadow mode; PR-C maps calibration-fetch
exceptions to fail-open WATCH, never a lane crash.

## Needs independent validation (<90% sure when reviewed)

1. Exact promotion mechanism for `news_article:positive:h15d` on 6/11 (stale-PF
   vs sticky flag) — run-log archaeology; doesn't change the fix.
2. Whether tariff/clinical pre-6/11 inflation came from floor applications or
   pre-FIX-1 increments (C1 repairs both).
3. **cron-job.org provisioned state vs `bootstrap_cronjob_org.py`** — console
   check before any re-run (re-running as-is risks the 2× doubling). [M7]
4. Applied-state of `sql/0010/0018/0040` indexes in live DB (`pg_indexes` query).
5. Whether the backtester ever overwrote a live weight row (diff
   `stock_agent_weights` dates vs dispatch dates 5/21, 5/26, 6/04). [M1]

## Recommended next-PR order (post-Criticals)

Per the reviewer's repair order, remaining: finish **H4** (display/report drift)
→ **H6** (risk pagination, helper exists) → **H1** (effective-n, design-heavy)
→ **H2** (after-hours anchor) → **H7** (watchdog alignment) → Mediums
opportunistically (**M2/M3** before the 2026-07-06 funnel re-run). Each a
separate PR with its own measurement gate.
